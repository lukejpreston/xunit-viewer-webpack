* replace all css with aphrodite
* inject data using the build and start scripts
* change the cli to run webpack
* add an input for running in the browser - auto updates, removes all old data
* get sockets working